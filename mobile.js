let img =document.querySelector(".img");
let countianer=document.querySelector(".countianer")
function phones(src) {
    img.src = src;
}
function color(color) {
    countianer.style.background= color;
    
}